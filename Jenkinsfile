pipeline {
  agent any

  stages {
    stage('Clone') {
      steps {
        echo "code done"
      }
    }

    stage('Build Docker Image') {
      steps {
      echo "Build Docker Done"
      }
    }

    stage('Deploy to K8s') {
      steps {
      echo "Deploy done"
      }
    }
  }
}
